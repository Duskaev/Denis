def bubble_sort(lst): # Создаем фунцию со списком
    One=1  #Опредеяем счетчик с 1
    while One<len(lst): # соритуем пока значение счетчика меньне длины списка
    	if lst[i]>lst[i+1]:  #сравниваем список
    		lst[i], lst[i+1]=lst[i+1], lst[i] #меняем местани

    	One+=1 #счетчик
    retrum lst #возвращаем отсортированный список